{
  "name": "guardian-sdk",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/sdk && npm run build -w packages/bsc",
    "typecheck": "npm run build -w packages/sdk && npm run typecheck --workspaces --if-present",
    "test": "npm test -w packages/bsc"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "JaimeToca",
  "license": "MIT"
}
